Cart.update(<%= current_order.items.sum(&:quantity) %>)
product = $("#product_<%= @item.product.id %>")
product.html("<%= j render @item.product %>")
product.find('.caption').effect('highlight', {}, 2000)