<%= form_for product.cart_items.build do |f| %>
  <%= f.hidden_field :product_id %>
  <%= f.number_field :quantity, value: 1, min: 1, class: 'input-mini' %>            
  <button class="btn btn-success"><i class="icon-shopping-cart icon-white"></i> Add Cart</button>
<% end %>