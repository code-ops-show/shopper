<%= semantic_form_for [:admin, @category], html: { multipart: true } do |f| %>
  <fieldset class="inputs">
    <ol>
      <li><%= f.input :name %></li>
      <li><%= f.input :description %></li>
    </ol>
  </fieldset>
  <%= f.submit %>
<% end %>